/**
 * Barrel file — same client as `./supabaseClient.js`.
 * Prefer: `import supabase from './supabaseClient'` or `import { supabase } from './supabase'`.
 */
export { default, isSupabaseConfigured, supabase } from './supabaseClient'
