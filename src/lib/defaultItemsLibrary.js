/**
 * Offline fallback when localStorage has no items library yet.
 * Use Admin → Import default removals catalogue for the full professional list.
 * Legacy categories (Appliances, Bedroom, Boxes, Living) are intentionally omitted.
 *
 * @type {Array<Record<string, unknown>>}
 */
export const DEFAULT_ITEMS_LIBRARY = []
