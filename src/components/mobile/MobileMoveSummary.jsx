import MobileQuoteMoveSummary from './MobileQuoteMoveSummary'

/** Mobile quote wizard move summary — unified card on all steps. */
export default function MobileMoveSummary(props) {
  return <MobileQuoteMoveSummary {...props} />
}
